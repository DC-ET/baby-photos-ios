import SwiftUI

// Theme configuration matching Android's Material3 purple/pink palette.
// On iOS, we rely on the system accent color (.tint) and standard SwiftUI styling.
// Custom theme colors are defined in Color+ext.swift for future use.
